# Sema — spec package for Claude Code

Drop this folder at the root of the new repo.

- `CLAUDE.md` — project memory, loaded every Claude Code session.
- `docs/SPEC.md` — product source of truth.
- `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/CONVERSATION_ENGINE.md`, `docs/INTEGRATIONS.md` — how it's built.
- `docs/SAFETY.md`, `docs/COMPLIANCE.md` — binding constraints.
- `docs/TESTING.md`, `docs/BUILD_PLAN.md` — how to verify and in what order to build (11 phases, one session opener template).
- `docs/adr/` — the five locked decisions.

Start Session 1 with the opener in `docs/BUILD_PLAN.md`, Phase 0.
